---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  ansible-lint .
  yamllint .
platforms:
  - name: centos8
    image: quay.io/centos/centos:7
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible

